coverage:
  status:
    project:
      default:
        enabled: yes
        threshold: 0
        informational: true
    patch:
      default:
        enabled: yes
        threshold: 0
        informational: true
