---
import Layout from '@layouts/Layout.astro';
import {Vault} from "@components/Vault"

import type { TAddress } from '@types';

const { vault,network } = Astro.params;
---
  <Layout title="Vault">
    <div class="my-vault">
      <Vault vault={vault as TAddress} network={network} client:only="react" />
    </div>
  </Layout>

<style is:global>
  .what-to-build table td {
      padding: 0 20px;
  }
  .what-to-build table tbody tr:hover td {
      background-color: #2f1755;
  }
  .what-to-build {
      font-size: 1rem;
  }
</style>
